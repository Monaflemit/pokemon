/*let table = document.getElementsByTagName('tr'); // Liste de tr (donc chaque ligne de ton tableau sauf la toute permière qui est les infos)
console.log(table);
// On boucle chaque ligne
for (let i = 0; i < table.length; i++) {
    let color = table[i][1].innerHTML; // On récupère le texte dans la deuxième colone (type)
    table[i][1].style.color = color; // On ajoute la coleur dans le type
};*/


